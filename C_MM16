#include <iostream>
#include <cmath>
using namespace std;

int main() {
    int x, y;
    cin >> x >> y;

    int dis = static_cast<int>(sqrt(x * x + y * y));

    if (dis <= 100)
        cout << "inside" << endl;
    else
        cout << "outside" << endl;

    return 0;
}
