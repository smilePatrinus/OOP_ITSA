#include <iostream>
#include <iomanip>
#include <cmath>
using namespace std;

int main() {
    double c, f;
    while (cin >> c) {
        f = c * 9 / 5 + 32;
        cout << fixed << setprecision(1) << round(f * 10) / 10 << endl;
    }
    return 0;
}
