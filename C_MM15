#include <iostream>
using namespace std;

int main() {
    int X, Y;
    while (cin >> X >> Y) {
        if (X > 100 || Y > 100) {
            cout << "outside" << endl;
        } else {
            cout << "inside" << endl;
        }
    }
    return 0;
}
