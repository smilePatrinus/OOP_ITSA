#include <iostream>
#include <iomanip>
#include <cmath>
using namespace std;

int main() {
    double up;

    while (cin >> up) {
        up *= 1.6;
        cout << fixed << setprecision(1) << round(up * 10) / 10 << endl;
    }

    return 0;
}
